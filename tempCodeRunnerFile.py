   # if self.rect.colliderect(block.rect):
        #     if self.rect.bottom >= block.rect.top and speed_y > 0:
        #         self.rect.bottom = block.rect.top  
        #         speed_y = -max_speed_y  